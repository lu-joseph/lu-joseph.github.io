name: Web deploy workflow
on:
  push:
    branches:
    - main
jobs:
  Job1:
    runs-on: ubuntu-latest
    steps:
      - run: npm install
      - run: npm run build
      - run: rm -rf docs
      - run: mv build docs
      - run: echo josephlu.ca >> docs/CNAME
